# ============================================
# WAYFARIAN MOBILE APP - ENVIRONMENT VARIABLES
# ============================================
# 
# SECURITY WARNING: Never commit this file with real values!
# Copy this file to .env and fill in your actual credentials.
# Keep your .env file in .gitignore at all times.
#
# To get started:
# 1. Copy this file: cp .env.example .env
# 2. Fill in all required values
# 3. Never commit the .env file
# ============================================

# ============================================
# FIREBASE CLIENT CONFIGURATION (REQUIRED)
# ============================================
# Get these from Firebase Console:
# 1. Go to Project Settings > General
# 2. Scroll to "Your apps" section
# 3. Select your web app or create one
# 4. Copy the config values
#
# IMPORTANT: All EXPO_PUBLIC_FIREBASE_* variables are REQUIRED
# The app will throw an error if any are missing
#
EXPO_PUBLIC_FIREBASE_API_KEY="your-firebase-api-key"
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
EXPO_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project.firebasestorage.app"
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
EXPO_PUBLIC_FIREBASE_APP_ID="your-app-id"

# ============================================
# GOOGLE OAUTH CONFIGURATION
# ============================================
# Required for Google Sign-In functionality
# Get from: https://console.cloud.google.com/apis/credentials
#
# Steps:
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URIs:
#    - https://auth.expo.io/@your-username/your-app-slug
#    - http://localhost:19006
# 3. Copy the Client ID
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID="your-web-client-id.apps.googleusercontent.com"

# ============================================
# API CONFIGURATION
# ============================================
# Backend API endpoints
# Development: Use your local machine's IP address
# Production: Use your deployed API URL
#
# To find your local IP:
# - Windows: ipconfig (look for IPv4 Address)
# - Mac/Linux: ifconfig (look for inet)
#
# Development examples:
# EXPO_PUBLIC_API_URL="http://192.168.1.100:3001/api"
# EXPO_PUBLIC_API_URL="http://10.0.0.50:3001/api"
#
# Production example:
# EXPO_PUBLIC_API_URL="https://api.yourdomain.com/api"
EXPO_PUBLIC_API_URL="http://localhost:3001/api"
EXPO_PUBLIC_SOCKET_URL="http://localhost:3001"

# ============================================
# SENTRY ERROR TRACKING (Recommended)
# ============================================
# Error monitoring for React Native app
# Sign up at: https://sentry.io/
#
# Steps:
# 1. Create a new React Native project in Sentry
# 2. Copy the DSN from Project Settings > Client Keys
# 3. Paste it below
#
# Note: Use a DIFFERENT project than your server!
# Leave empty to disable Sentry (not recommended for production)
EXPO_PUBLIC_SENTRY_DSN=""
# Send errors from development environment (usually false)
EXPO_PUBLIC_SENTRY_ENABLE_IN_DEV=false

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Google Maps API Key (for mobile app)
# Note: This is different from server-side key
# Restrict to Android/iOS app signatures in Google Console
# GOOGLE_MAPS_API_KEY="your-mobile-api-key"

# Mapbox Access Token (if using Mapbox)
# MAPBOX_ACCESS_TOKEN="pk.your-mapbox-token"

# Auto-pause journey on app restart (development)
EXPO_PUBLIC_AUTOPAUSE_ON_BOOT=true

# ============================================
# SECURITY NOTES
# ============================================
# 
# 1. FIREBASE KEYS ARE PUBLIC
#    - Firebase client keys are meant to be public
#    - Security is enforced by Firebase Security Rules
#    - Never put Firebase Admin SDK keys here!
#
# 2. API KEYS IN MOBILE APPS
#    - Keys in React Native apps can be extracted
#    - Restrict keys in provider consoles:
#      * Firebase: Set up Security Rules
#      * Google Maps: Restrict to app signatures
#      * APIs: Use server-side proxies for sensitive calls
#
# 3. NEVER HARDCODE CREDENTIALS
#    - Always use environment variables
#    - Different keys for dev/staging/production
#    - Rotate keys regularly
#
# 4. .ENV FILES
#    - Never commit .env to version control
#    - Keep .env in .gitignore
#    - Use .env.example as template only
#
# ============================================

# ============================================
# DEPLOYMENT CHECKLIST
# ============================================
# Before deploying to production:
# [ ] All EXPO_PUBLIC_FIREBASE_* variables are set
# [ ] EXPO_PUBLIC_API_URL points to production API
# [ ] EXPO_PUBLIC_SENTRY_DSN is configured
# [ ] API keys are restricted in provider consoles
# [ ] Different keys than dev/staging
# [ ] No localhost URLs remain
# [ ] Test authentication flows
# [ ] Test error reporting to Sentry
# ============================================
